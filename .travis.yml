language: php

php:
  - '7.0'
  - '7.1'

matrix:
    allow_failures:
        - php: 7.0
        - php: nightly
        - php: hhvm

sudo: false

install: travis_retry composer install --no-interaction --prefer-source

script: vendor/bin/phpunit --coverage-clover=coverage.clover Test.php